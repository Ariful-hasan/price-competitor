[www]
user = www-data
group = www-data
listen = 0.0.0.0:${PHP_FPM_PORT}

pm = dynamic
pm.max_children = 20
pm.start_servers = 5
pm.min_spare_servers = 5
pm.max_spare_servers = 10
pm.max_requests = ${PHP_FPM_NGINX_MAX_REQUESTS}

; Ensure environment variables are accessible to PHP
clear_env = no
